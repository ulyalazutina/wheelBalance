import { InputModal } from "./Input.styled";

function Input({ type, placeholder, id }) {
    return <InputModal type={type} placeholder={placeholder} id={id} />;
}

export default Input;

// инпуты для модалок
